import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    padding: 8,
    borderRadius: 6,
    borderWidth: 1.5,
    flexDirection: "row",
    gap: 6,
    justifyContent: "center",
    alignItems: "center",
  },

  label: {
    fontWeight: "700",
    color: "#fff",
  },
});
