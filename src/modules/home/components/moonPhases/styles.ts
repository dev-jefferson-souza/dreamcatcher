import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  card: { marginBottom: 12, minHeight: 120, justifyContent: "center" },

  moonContainer: {
    height: 100,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    alignContent: "space-between",
  },

  informationText: {
    color: "#fff",
    fontSize: 16,
    fontWeight: "600",
  },
});
